version: '3.9'

services:
  bot:
    build: .
    container_name: if-tg-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '3000:3000' # optional, for health endpoint
    # If you prefer pulling from a registry, replace build with image: your-registry/if-tg-bot:latest
